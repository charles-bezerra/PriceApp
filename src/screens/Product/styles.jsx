import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 22,
  },
  titleModal: {
    overflow: "hidden",
    fontSize:24,
    fontWeight:'bold',
  },
  subtitleModal: {
    fontSize: 18,
    fontWeight: "normal"
  }
});

export default styles;